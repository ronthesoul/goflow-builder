name: goflowBuilder-Test

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:
    runs-on: ubuntu-latest

    env:
      SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
      NOTIFY: ".github/workflows/notify.sh"

    steps:
    - uses: actions/checkout@v4
    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.22.2'

    - name: Notify Start
      run: |
          chmod +x $NOTIFY
          $NOTIFY "Starting ${{ github.event.repository.name }} Test - Commit: ${{ github.event.head_commit.message }}" $SLACK_WEBHOOK

    - name: Build
      run: |
        $NOTIFY "Starting Step - Build" $SLACK_WEBHOOK
         if go build -v ./...; then
           $NOTIFY "Step - Build ✅ Success" $SLACK_WEBHOOK
           else
            $NOTIFY "Step - Build ❌ Failed" $SLACK_WEBHOOK
          fi
    - name: Test
      run: | 
        $NOTIFY "Starting Step - Test" $SLACK_WEBHOOK
        if go test -v ./...; then 
            $NOTIFY "Step - Test ✅ Success" $SLACK_WEBHOOK
           else
            $NOTIFY "Step - Test ❌ Failed" $SLACK_WEBHOOK
          fi
    - name: Final pipeline result
      if: success()
      run: |
          $NOTIFY "🚀 Pipeline completed successfully!" $SLACK_WEBHOOK
    - name: Final pipeline failure
      if: failure()
      run: |
          $NOTIFY "🔥 Pipeline failed!" $SLACK_WEBHOOK

